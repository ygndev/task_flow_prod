# =============================================================================
# TaskFlow - Environment Variables Example
# =============================================================================
# This file lists all environment variables needed to run TaskFlow locally.
# Do NOT commit a real .env file containing secrets.
#
# Usage:
#   1. Copy this file to .env at the root:  cp .env.example .env
#   2. Copy app-specific examples and fill in your values:
#      cp apps/web/.env.example apps/web/.env
#      cp apps/api/.env.example apps/api/.env
#   3. Edit .env files with your real values (never commit them).
# =============================================================================

# -----------------------------------------------------------------------------
# Firebase Client (Frontend - apps/web)
# From Firebase Console > Project Settings > General > Your apps
# -----------------------------------------------------------------------------
VITE_FIREBASE_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012
VITE_FIREBASE_APP_ID=1:123456789012:web:abcdef1234567890abcdef

# -----------------------------------------------------------------------------
# Backend API (Frontend - apps/web uses this to call the API)
# -----------------------------------------------------------------------------
VITE_API_BASE_URL=http://localhost:4000

# -----------------------------------------------------------------------------
# Firebase Admin (Backend - apps/api)
# Path to the service account JSON key file (relative to apps/api or absolute)
# Download from Firebase Console > Project Settings > Service accounts
# -----------------------------------------------------------------------------
FIREBASE_SERVICE_ACCOUNT_PATH=serviceAccountKey.json
