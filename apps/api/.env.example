# Server Configuration
PORT=4000
NODE_ENV=development

# Firebase Admin SDK
# Option 1: Path to service account JSON file (recommended for local development)
FIREBASE_SERVICE_ACCOUNT_PATH=

# Option 2: Service account key as JSON string (alternative)
FIREBASE_SERVICE_ACCOUNT_KEY=

# If neither is set, will use application default credentials (gcloud CLI or GOOGLE_APPLICATION_CREDENTIALS)

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
VITE_API_URL=http://localhost:4000

